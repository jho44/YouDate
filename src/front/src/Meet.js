import React from "react";

/**
 * The first page we see for now -- soon to be Meet page. Takes no
 * parameters (yet).
 *
 * @returns {HTML} Meet page, soon to include Profile page
 *
 * @class
 */
const Meet = (props) => (
  <div>
    <h2>Meet {console.log(props)}</h2>
  </div>
);

export default Meet;
